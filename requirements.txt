pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
jinja2>=3.1

# optional — only needed to run broken_pipelines/ and the multi-family tests
xgboost>=2.0

# dev
pytest>=7.4

# streamlit UI (app.py)
streamlit>=1.30
